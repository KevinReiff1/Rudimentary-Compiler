{
print((a!/*hello*/=0))  /*allowed (but this is really annoying, since it makes it so we can't just check the next character after the '!' to check for validity)*/
print((0!=a )) /*allowed*/
print("") /*allowed*/
print(0) /*allowed*/


} $